{{#each @companies as |company|}}
  <LinkTo @model={{company}} @route='home.order-list'>
    <button type='button' class='btn btn-primary btn-lg'>
      {{company.name}}
    </button>
  </LinkTo>
{{/each}}