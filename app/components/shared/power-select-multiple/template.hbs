<PowerSelectMultiple
  @searchField='name'
  @searchEnabled={{true}}
  @options={{@options}}
  @selected={{@selected}}
  @placeholder={{@placeholder}}
  @onChange={{@onChange}}
  as |value|
>
  {{value}}
</PowerSelectMultiple>